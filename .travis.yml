language: php

sudo: false

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - nightly
  - hhvm

matrix:
  allow_failures:
    - php: nightly
  fast_finish: true

install:
  - composer self-update
  - composer --prefer-source install

script:
  - ./vendor/bin/phpunit -v --debug
